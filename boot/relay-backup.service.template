[Unit]
Description=Relay workspace backup (DB snapshot: sqlite/pg_dump + shared/ + relay.log + relay.env)
After=network.target

[Service]
Type=oneshot
ExecStart=__CURRENT_LINK__/scripts/backup.sh
Nice=10
